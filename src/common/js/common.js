/**
* @desc common
*/
window.addEventListener('DOMContentLoaded', function(){
  var bodyWidth = $('html, body').width();
  var fontSize = bodyWidth / 6.4;
  $('html').css('font-size', fontSize);
}, false);
